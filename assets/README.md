# Assets 目录

此目录用于存放应用资源文件。

## 需要的文件

- `icon.icns`: macOS 应用图标文件
  - 可以使用在线工具将 PNG 转换为 ICNS
  - 推荐尺寸：512x512 或 1024x1024

- `dmg-background.png`: DMG 安装包背景图片（可选）
  - 推荐尺寸：600x400

## 图标生成

可以使用以下工具生成 ICNS 文件：
- 在线工具：https://cloudconvert.com/png-to-icns
- 命令行：使用 `iconutil` 命令

